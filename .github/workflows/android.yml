name: Android Build
on:
  workflow_dispatch:
  workflow_call:
jobs:
  build:
    name: Android Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@main
        with:
          fetch-depth: 0

      - name: Setting up Haxe
        uses: krdlab/setup-haxe@v2
        with:
          haxe-version: 4.3.7

      - name: Setup Android NDK
        id: setup-ndk
        uses: nttld/setup-ndk@main
        with:
          ndk-version: r21e

      - name: Restore Cache
        uses: actions/cache@v4
        with:
          key: cache-build-android-${{ github.run_id }}
          path: |
            ~/.gradle
            .haxelib/
            /home/runner/haxelib/
            ~/.haxelib/
            export/release/android/haxe/
            export/release/android/obj/
          restore-keys: |
            cache-build-android-none

      - name: Configure Git auth for private haxelibs
        env:
          GH_PAT: ${{ secrets.GH_PAT }}
        if: env.GH_PAT != ''
        run: |
          git config --global url."https://x-access-token:${GH_PAT}@github.com/HomuHomu833-haxelibs/".insteadOf "https://github.com/HomuHomu833-haxelibs/"

      - name: Installing/Updating libraries
        run: |
          rm -rf .haxelib
          mkdir -p .haxelib
          haxelib setup || true
          retry_install() {
            local lib="$1"
            local version="$2"
            local timeout_sec=600
            local max_attempts=5
            local attempt=1
            while [ $attempt -le $max_attempts ]; do
              echo "Attempt $attempt/$max_attempts: installing $lib${version:+ }$version (timeout ${timeout_sec}s)"
              if timeout $timeout_sec haxelib install --always "$lib" $version; then
                echo "✅ Successfully installed $lib"
                return 0
              fi
              echo "❌ Installation of $lib failed or timed out, retrying in 30s..."
              sleep 30
              attempt=$((attempt+1))
            done
            echo "⚠️ Failed to install $lib after $max_attempts attempts."
            return 1
          }
          echo "=== Installing core libraries ==="
          retry_install hxcpp
          retry_install openfl
          retry_install hxjava

          echo "=== Installing Android native extension ==="
          if ! haxelib list | grep -q "extension-androidtools"; then
            haxelib git extension-androidtools https://github.com/LimeExtensions/extension-androidtools.git || \
            retry_install extension-androidtools
          fi

          echo "=== Installing lime ==="
          if retry_install lime 7.9.0; then
            echo "✅ lime 7.9.0 installed"
          else
            echo "lime 7.9.0 failed, trying 8.2.1..."
            if retry_install lime 8.2.1; then
              echo "✅ lime 8.2.1 installed"
            else
              echo "Both versioned installations failed. Cloning lime from GitHub..."
              if haxelib git lime https://github.com/openfl/lime; then
                echo "✅ lime installed from git"
              else
                echo "❌ All lime installation methods failed. Exiting."
                exit 1
              fi
            fi
          fi
          echo "=== Installing hxvlc (optional) ==="
          haxelib install hxvlc || echo "hxvlc skipped."
          echo "=== Running project setup script ==="
          haxe -cp commandline -D analyzer-optimize --run Main setup -s || echo "Setup script finished."
          echo "=== Installed haxelibs ==="
          haxelib list

      - name: Patch hscript bug
        run: |
          find . -name "Interp.hx" -path "*/hscript/*" -exec sed -i 's/return Config\.IMPORT_BLACKLIST;/return [];/g' {} \;
          find . -name "Interp.hx" -path "*/hscript/*" -exec sed -i 's/return Config\.importBlacklist;/return [];/g' {} \;

      - name: Fix missing Android extern classes
        run: |
          # 批量创建缺失的安卓系统类extern定义，解决Type not found编译错误
          mkdir -p source/android/os
          mkdir -p source/android/content

          # android.os.Build
          cat > source/android/os/Build.hx << 'EOF'
          package android.os;

          extern class Build {
              public static var MODEL:String;
              public static var MANUFACTURER:String;
              public static var PRODUCT:String;
              public static var DEVICE:String;
              public static var VERSION:Version;
          }

          extern class Version {
              public static var SDK_INT:Int;
              public static var RELEASE:String;
              public static var CODENAME:String;
          }
          EOF

          # android.os.Environment
          cat > source/android/os/Environment.hx << 'EOF'
          package android.os;

          extern class Environment {
              public static var MEDIA_MOUNTED:String;
              public static var MEDIA_UNMOUNTED:String;
              public static function getExternalStorageDirectory():String;
              public static function getExternalStorageState():String;
              public static function getDataDirectory():String;
              public static function getDownloadCacheDirectory():String;
              public static function getRootDirectory():String;
          }
          EOF

          # android.content.Context
          cat > source/android/content/Context.hx << 'EOF'
          package android.content;

          extern class Context {
              public function getExternalFilesDir(type:String):String;
              public function getFilesDir():String;
              public function getPackageName():String;
              public function getContentResolver():ContentResolver;
              public function getSharedPreferences(name:String, mode:Int):SharedPreferences;
          }

          extern class ContentResolver {}
          extern class SharedPreferences {}
          EOF

          # android.Settings
          cat > source/android/Settings.hx << 'EOF'
          package android;

          extern class Settings {
              public static var System:SystemSettings;
          }

          extern class SystemSettings {
              public static var SCREEN_BRIGHTNESS:String;
              public static var SCREEN_OFF_TIMEOUT:String;
          }
          EOF

          # android.Permissions
          cat > source/android/Permissions.hx << 'EOF'
          package android;

          extern class Permissions {
              public static var READ_EXTERNAL_STORAGE:String;
              public static var WRITE_EXTERNAL_STORAGE:String;
              public static var CAMERA:String;
              public static var RECORD_AUDIO:String;
              public static var INTERNET:String;
              public static var ACCESS_NETWORK_STATE:String;
              public static var WAKE_LOCK:String;
              public static var VIBRATE:String;
          }
          EOF

      - name: Configure Android
        run: |
          haxelib run lime setup -alias -y
          haxelib run lime config ANDROID_SDK $ANDROID_HOME
          haxelib run lime config ANDROID_NDK_ROOT $ANDROID_NDK_HOME
          haxelib run lime config JAVA_HOME $JAVA_HOME_17_X64
          haxelib run lime config ANDROID_SETUP true

      - name: Fix Haxelib (optional)
        run: |
          haxelib set flixel git
          haxelib set flixel-addons git
          haxelib set flxanimate git
          haxelib set hxdiscord_rpc git

      - name: Building the game
        run: |
          haxelib run lime build android -final -D ONLY_ARM64

      - name: Uploading artifact
        uses: actions/upload-artifact@v4
        with:
          name: Dustin Android
          path: export/release/android/bin/app/build/outputs/apk/*

      - name: Clear Previous Cache
        uses: actions/github-script@v6
        with:
          script: |
            const caches = await github.rest.actions.getActionsCacheList({
              owner: context.repo.owner,
              repo: context.repo.repo,
            })
            for (const cache of caches.data.actions_caches) {
              if (cache.key == "cache-build-android") {
                console.log('Clearing ' + cache.key + '...')
                await github.rest.actions.deleteActionsCacheById({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  cache_id: cache.id,
                })
                console.log("Cache cleared.")
              }
            }

      - name: Upload New Cache
        uses: actions/cache@v4
        with:
          key: cache-build-android
          path: |
            ~/.gradle
            .haxelib/
            /home/runner/haxelib/
            ~/.haxelib/
            export/release/android/haxe/
            export/release/android/obj/
          restore-keys: |
            cache-build-android-none
